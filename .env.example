# Root environment template (no secrets)
# This repo uses per-app env files:
# - backend/.env
# - frontend/.env
#
# Copy templates and fill local values:
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env
