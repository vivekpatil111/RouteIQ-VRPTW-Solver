# ILS backend only (Option A): use this in a separate venv/container with pyvrp 0.13+.
# Same deps as main backend but pyvrp>=0.13 (ILS) instead of pyvrp==0.6.3 (HGS).
# Python 3.11 or 3.12 recommended.
certifi>=2024.0.0
ortools>=9.12.0
pandas>=2.2.0
pydantic>=2.5.0
pyvrp>=0.13
tabulate==0.9.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
