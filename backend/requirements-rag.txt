# Optional: RAG over algorithm docs (install with: pip install -r requirements-rag.txt)
# Requires: pip install -r requirements.txt first
langchain-core>=0.3.0
langchain-community>=0.3.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
pypdf>=4.0.0
